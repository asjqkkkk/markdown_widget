name: markdown_widget
description: markdown widget support TOC、video、highlight code...
version: 1.0.0
author: Old Li <agedchen@gmail.com>
homepage: https://github.com/asjqkkkk/markdown_widget

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  markdown: ^2.1.3
  #highlight code
  flutter_highlight: ^0.6.0
  html: ^0.14.0+3
  # for 'video' tag
  video_player_web: ^0.1.2+1
  video_player: ^0.10.5
  chewie: ^0.9.10
  # for TOC
  scrollable_positioned_list: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
